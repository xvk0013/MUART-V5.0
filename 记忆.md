# MUART V5.0 多目标水声识别项目交接记忆

> 更新时间：2026-09-06  
> 用途：新开对话时，请先让 Codex 完整阅读本文件，再继续修改代码或安排实验。  
> 当前阶段：V6-A2 与 V6-D1 已完成正式冻结 Val 对照；D1 未支持录音曝光偏置假设。下一步进入 DeepShip 四种三类别组合及数据生成前后可识别性审计。

---

## 1. 项目目标与当前结论

任务是在物理可信的合成水声数据上识别三类船舶：`Tanker`、`Cargo`、`Tug`。每个样本只可能属于以下 7 个集合：

1. noise（零目标）
2. Tanker
3. Cargo
4. Tug
5. Tanker+Cargo
6. Tanker+Tug
7. Cargo+Tug

没有三目标样本。主指标为多标签严格完全匹配率 EMR；只要任意一个类别多报或漏报，整条样本即判错。

截至目前最重要的结论：

- 模型并非完全没有提升空间，但 V5 上继续堆叠普通门控、融合支路或通用增强的收益已经接近耗尽。
- TC（Target Count）数量预测已经接近饱和，当前主要瓶颈不是“有几个目标”，而是“目标具体是谁”。
- 最大的稳定错误模式是 **Cargo 与 Tanker 的相互混淆**，不是 Cargo 与 Tug。
- Tug 是最容易识别的类别；Cargo 是最困难的类别。
- 下一步不应恢复成两套相互独立的大网络 A/B，而应使用同一个共享主干和共享类别 Query，仅在末端使用小型条件分类头。
- 条件集合分类头需要参与训练并反向塑造共享表征；只把当前 logits 换一种解码方式无法达到 85%。
- 数据侧需要提升“有效多样性”，尤其是 Cargo/Tanker 的原始录音、船舶身份和困难物理条件，而不是简单增加重复样本或类别权重。
- V6-D1 已证明把 Train 改为逐录音均匀抽样不能突破 Cargo 单目标；录音曝光偏置不是当前 Cargo/Tanker 边界问题的主要解释。
- D1 对双目标和总体 EMR 有小幅正向迁移，但单目标 Cargo/Tanker 反而略差且过拟合更严重，因此不能把总体 EMR 上涨解释成核心问题已解决。

---

## 2. 两台电脑上的重要路径

### 2.1 当前工作区（写代码的电脑）

```text
E:\Paper\Code\MUART V5.0
E:\Paper\Code\MUART V5.0\data_gen_v2
```

### 2.2 训练电脑

代码目录：

```text
D:\LSJ\Code\UART\MUART V5.0
```

训练输出目录与代码目录并列，文件夹名称本身是 `MUART V5.0_runs`：

```text
D:\LSJ\Code\UART\MUART V5.0_runs
```

不要误写成：

```text
D:\LSJ\Code\UART\MUART V5.0\_runs
```

当前数据目录：

```text
D:\LSJ\Code\UART\MUART V4.0\data_cargo_sl_sir15\SS2_bp12\dataset
```

PowerShell 和 Python 命令中可以使用 `/`，可避免反斜杠复制错误。下划线 `_` 前不需要增加转义符。

当前最小运行包已将 `data_gen_v2` 放入 `MUART V5.0` 内部；整包迁移时必须保留其中的 `.arr_gen`。外部大数据与训练输出仍通过运行参数指定，不放入代码包。

在 VS Code 中选好包含 PyTorch 的 Conda 环境后，不需要额外定义所谓“宏”。实验设置通过命令行参数和 checkpoint 内保存的 resolved config 确定。

---

## 3. 当前数据生成协议

当前数据生成主线位于：

```text
E:\Paper\Code\data_gen_v2
```

已确认的重要物理规则：

- 生成条件名为 `SS2_bp12`。
- 当前配置只有 `wind=2.5 m/s`，`shipping_factor=0.2`。
- 当前结果不能直接称为“海况 3 结果”。如果目标真的是 Sea State 3，必须重新定义、生成并冻结相应物理条件和独立测试集。
- 类别与生成器标签：Cargo=`0`、Tanker=`1`、Tug=`2`。
- 模型内部 `class_names` 顺序为 Tanker、Cargo、Tug，因此存在位置映射：`{0:1, 1:0, 2:2}`。
- 固定 SL：每条原始录音只采样一次；均值 `[183,183,180.5]` dB re 1 μPa @1m，标准差 3 dB。
- ER 使用 `natural`，不截断、不修改固定 SL。
- 信道使用预计算 Bellhop `.arr` 到达文件。
- 噪声使用 Wenz 模型。
- 双目标在 Bellhop 和包络之后，按接收端 `[20,4000] Hz` 重叠区 SIR 条件抽样。
- SIR 分层为 `[0,5)`、`[5,10)`、`[10,15] dB`，占比 `45%/35%/20%`。
- SIR 只用于接受或重抽整个候选，不能通过修改 SL、ER 或信号幅度来“制造”目标 SIR。
- mix、signal、noise、s1、s2、s3 共用同一写盘缩放系数 `scale_c`。
- Train/Val/Test 采用录音级边界隔离；同一原始录音不得跨集合。
- 已知规模为 Train 11490、Val 2463、Test 2463；Val 中 noise=324、single=681、double=1458。
- `all_info.txt` 记录约 60 个审计字段，包括原始文件、距离、源深、接收深、SNR、ER、重叠区间和多种 SIR。

数据集本身采用了物理 SIR 条件分层，因此论文中应透明说明它是“指定物理可辨识区间下的条件分布”，不能宣称是未经条件化的自然海洋分布。

---

## 4. V5 当前模型结构与实际行为

当前主线大致包括：

- 双通道主输入：
  - 低频 LOFAR：0–1 kHz，高频率分辨率；
  - 宽带增量证据：1–4 kHz。
- TF-ConvNeXt 共享骨干。
- DEMON 1D 编码分支。
- Semantic Query Attention：每个类别一个可学习 Query。
- `TCResidualClassifier`：
  - base 多标签分类头；
  - TC 数量头（0/1/2）；
  - 0/1 与 2 目标残差专家；
  - 当前 TC/残差使用 detach 后的池化特征，属于只读探针，不塑造 backbone；
  - 推理时 TC 只决定选哪个残差，最终仍返回三个独立类别 logits。
- 历史验证按三个 sigmoid 概率分别使用固定阈值 0.5。
- 新增的比较脚本能够额外做 TC top-k 数量约束，但目前这仍是评估/诊断解码，不是训练时的结构化集合学习。

当前表现最好的 E8 配置：

- `feature=dual`
- `no-spec-gate`
- `dual_stream_fusion=False`
- DEMON 保留普通模式
- `normalization_mode=per_channel`
- `freq_shift_mode=off`
- `waveform_noise_mode=off`
- 不使用 speed perturb
- EMA 验证/保存
- 使用 WHITE 与 OFF 两位教师进行训练期集成蒸馏

checkpoint 重放必须优先读取 `ema_state_dict`；使用 `model_state_dict` 会与训练时保存的最佳 Val 指标不一致。

---

## 5. V5 消融探索与结论

### 5.1 SpectrumConditionedGate / E3

- 关闭频谱条件门控后，结果基本没有变化。
- 历史 `val_results.txt` 记录 E3 no-gate 最佳 Val EMR=`0.788063`，最佳 epoch=46。
- 曾误用一个保存 Val=`0.768169` 的旧/错误 checkpoint；比较脚本忠实重放后发现它确实只有 0.768169，不能代表 E3。
- 原始正确 E3 checkpoint 后来没有找到，因此做了白噪声控制重跑。
- 结论：门控不是当前性能瓶颈；当前主线关闭。

### 5.2 双流融合 / E4

- 低频和宽带分别通过共享权重 backbone，再在 Query 级融合。
- 最佳 Val EMR=`0.788469`。
- 相比 no-gate 基线没有有意义提升。
- 结论：不保留双流融合。

### 5.3 DEMON 低频切片 / E5

- 最佳 Val EMR=`0.788875`，最佳 epoch=26。
- 与基线几乎相同。
- 结论：不把 DEMON slice 作为主线改进。

### 5.4 白噪声增强关闭 / E6

WHITE 控制重跑（seed 42）：

- raw EMR=`0.786439`，best epoch=45。
- TC constrained=`0.813236`。

OFF（关闭波形白噪声）三种子：

| 模型 | raw EMR | TC constrained | true-count oracle |
|---|---:|---:|---:|
| E6 s42 | 0.789687 | 0.817296 | 0.817702 |
| E6 s43 | 0.793341 | 0.814860 | 0.816484 |
| E6 s44 | 0.785221 | 0.806334 | 0.806334 |

三种子均值：

- raw：约 `0.789417 ± 0.003319`
- TC constrained：约 `0.812830 ± 0.004700`

E6 三种子集成：

- probability mean raw=`0.797402`
- probability mean + TC=`0.819732`
- true-count oracle=`0.820138`
- 模型 oracle=`0.850589`
- all-model shared wrong=368（14.94%）

结论：关闭白噪声略有帮助，但单模型提升较小；TC 解码的收益远大于白噪声开关本身。当前关闭白噪声。

### 5.5 速度扰动 / E7

- 使用 `--speed-perturb`，±10%，训练期 100% 应用。
- 最佳 Val EMR=`0.7649`，best epoch=59。
- 相比主线明显下降。
- 结论：速度扰动即使形式上保持谐波比例，也不符合当前数据/任务的有效不变性；关闭，不再投入。

### 5.6 集成蒸馏 / E8

教师：

- WHITE s42：raw=`0.786439`，TC=`0.813236`
- OFF s42：raw=`0.789687`，TC=`0.817296`
- 两教师概率平均：raw=`0.795778`，+TC=`0.817702`

E8 三种子：

| 模型 | raw EMR | TC constrained | true-count oracle | TC count acc |
|---|---:|---:|---:|---:|
| E8 s42 | 0.798620 | 0.820544 | 0.820950 | 0.998376 |
| E8 s43 | 0.799026 | 0.818108 | 0.818514 | 0.998782 |
| E8 s44 | 0.797808 | 0.822980 | 0.825010 | 0.997564 |

三种子均值：

- raw：约 `0.798484 ± 0.000506`
- TC constrained：约 `0.820544 ± 0.001989`

E8 三种子集成：

- probability mean raw=`0.802680`（1977/2463）
- probability mean + TC=`0.827040`（2037/2463）
- probability mean + true-count=`0.827446`（2038/2463）
- raw model oracle=`0.857491`（2112/2463）
- all-model shared wrong=351（14.25%）
- ensemble+TC：noise=1.0000、single=0.7988、double=0.8018

结论：

- 蒸馏使 raw 性能提高约 0.9 个百分点，并显著降低种子方差。
- E8 单学生超过其两个教师的简单集成，证明蒸馏学习到了有效软信息。
- 继续增加同架构、仅不同 seed 的教师，预计收益有限，因为它们共享同一稳定偏差。
- 当前最高可部署验证结果是 E8 三种子集成 + TC 的 `82.704%`。
- 如果要求单模型，则 E8+TC 三种子平均约为 `82.054%`。

---

## 6. TC、残差专家与集成的最终判断

### 6.1 TC 已接近饱和

E8 三种子集成：

- 预测 TC 解码：2037 个正确；
- 使用真实目标数：2038 个正确。

真实数量只多救回 1 个样本。因此继续强化 TC 数量头几乎不可能把 82.7% 推到 85%。

### 6.2 当前 TC 没有真正参与最终集合建模

旧实现中 TC 只选择 `delta_01` 或 `delta_2`，最终仍输出三个独立 logits。独立 0.5 阈值可能输出与 TC 不一致的类别数。

后来增加 top-k 数量约束后，EMR 明显提高，证明数量先验有价值；但 true-count 与 predicted-count 几乎相同，也证明当前剩余错误是类别身份错误。

### 6.3 残差专家没有成为有效主贡献

`base+TC` 与 `routed+TC` 的差异很小且不稳定，有时 base 反而略好。V6 初始版本应移除残差专家或降为可选消融，不再把它作为核心贡献。

### 6.4 单模型与集成的定义

- E8 蒸馏学生 + TC：推理只加载一个学生 checkpoint，属于单模型系统。
- 教师集成只在训练期使用，不会让学生在推理时变成集成。
- E8 s42/s43/s44 概率平均 + TC：需要同时加载三个学生，属于推理期集成。
- 论文应分别报告无蒸馏基线、教师集成、蒸馏学生、蒸馏学生+TC，以及可选的多种子推理集成。这不是把蒸馏排除，而是明确每部分贡献和部署成本。

### 6.5 换数据集时是否需要重新蒸馏

- 如果新数据只是在同一任务、相同类别、相同预处理与相同分布下重新生成，现有学生可直接做推理基线。
- 若物理环境、海况、SIR、船舶录音或类别分布发生变化，现有学生和教师都可能产生域偏移。
- 为取得可靠最佳性能，应在新数据的 Train 上重新训练/微调教师和学生，并重新蒸馏；不能假定旧蒸馏收益自动迁移。
- 任何情况下 Test 都不能参与蒸馏、阈值选择或教师权重选择。

---

## 7. 最新逐集合与身份混淆诊断

比较脚本新增 `--set-diagnostics`，会输出：

- 七种合法集合的 raw、+TC、identity@true-count 和 TC-count；
- 单目标 top-1 身份混淆矩阵；
- 双目标 top-2 类别对混淆矩阵。

三个 E8 seed 的结果高度稳定。

### 7.1 单目标身份准确率范围

| 类别 | identity@top1 |
|---|---:|
| Tanker | 78.9%–81.1% |
| Cargo | 61.2%–64.3% |
| Tug | 95.6%–96.9% |

主要单目标错误：

- Cargo → Tanker：每个 seed 约 63–68 个，占 Cargo 的 27.8%–30.0%。
- Tanker → Cargo：每个 seed 约 30–35 个，占 Tanker 的 13.2%–15.4%。
- Cargo → Tug：约 18–20 个，占 7.9%–8.8%。
- Tug → Cargo：约 7–10 个，占 3.1%–4.4%。
- Tug → Tanker 几乎为 0。

因此用户关于“单目标性能不够好”的判断正确，但按当前标签映射，主冲突是 Cargo/Tanker，不是 Cargo/Tug。

### 7.2 双目标类别对准确率范围

| 类别对 | identity@top2 |
|---|---:|
| Tanker+Cargo | 84.6%–85.8% |
| Tanker+Tug | 80.9%–82.9% |
| Cargo+Tug | 70.6%–71.4% |

主要双目标错误：

- Cargo+Tug → Tanker+Tug：约 123–128 个，占 25.3%–26.3%。
- Tanker+Tug → Cargo+Tug：约 58–65 个，占 11.9%–13.4%。

这再次说明模型通常能够检测到 Tug，但难以确定另一个目标是 Cargo 还是 Tanker。

### 7.3 类别权重不是答案

Val 中：

- 每个单目标类别都是 227 个；
- 每个双目标类别对都是 486 个。

数量完全平衡，因此 Cargo 性能差不是简单的样本计数不平衡。提高 Cargo 权重可能只会把决策边界推向 Tanker，增加 Tanker → Cargo，而不会创造新的可分信息。

### 7.4 达到 85% 的差距

- 当前 E8 三种子集成+TC：2037/2463=`82.704%`。
- 达到不低于 85% 至少需要 2094/2463=`85.018%`。
- 还差 57 个正确样本。
- noise 已经 324/324 全对。
- 非噪声当前为 1713/2139=`80.08%`，需要达到 1770/2139=`82.75%`。

Cargo/Tanker 相关的稳定替换错误约有 279 个。若能可靠纠正其中约 20%，理论上就足以跨过 85%；但不能通过 oracle 选择或验证集过拟合实现。

raw model oracle=`85.749%` 只说明不同模型错误并非完全重合。它假设事先知道哪个模型正确，不可部署，也不能作为实际性能报告。

---

## 8. V6 最终确定的模型方向

### 8.1 不再使用彼此割裂的完整 A/B 网络

最初设想是：TC 先分零/单/双目标，0/1 目标进入网络 A，双目标进入网络 B。该方案失败的主要原因是：

- A/B 之间缺乏共享；
- 每个专家只看到子集数据；
- 大网络参数多、子集样本少；
- 单目标、双目标知识无法互相迁移；
- 最终单目标、双目标及总体性能均下降。

V6 保留“按目标数条件化”的思想，但只在末端分头，绝大多数参数共享。

### 8.2 推荐结构：Hierarchical Conditional Set Classifier

```text
全部零/单/双目标训练数据
             │
             ▼
共享 dual + DEMON + TF-ConvNeXt 主干
             │
             ▼
共享类别 Query：Tanker / Cargo / Tug
       ├──────────┬───────────────┐
       ▼          ▼               ▼
    TC 数量头   单目标三分类头   双目标类别对三分类头
    k=0/1/2    T / C / G       T+C / T+G / C+G
       └──────────┴───────────────┘
                    │
                    ▼
          七个合法集合的联合概率
```

联合集合分数：

```text
score(S) = log p_TC(|S| | x) + log p_identity(S | |S|, x)
```

在 7 个合法集合中直接取最大值，不再依赖三个独立 0.5 阈值。

重要实现原则：

- TC、单目标头、双目标头和类别 Query 共享同一个 backbone。
- 条件集合主头不能 detach，必须参与共享表征反向传播。
- 单目标头只包含很小的三分类层，而不是一套完整网络 A。
- 双目标头只包含很小的三种类别对分类层，而不是一套完整网络 B。
- 原始三类别 BCE 作为全样本辅助损失保留，防止层次头割裂。
- 双目标 pair score 应共享单类证据，可写成两个类别分数之和加一个很小的交互项，避免完全独立的 pair 网络。
- `k=0` 时集合固定为空；无需为 noise 建一个大专家。

### 8.3 推荐损失

```text
L = L_set
  + λ_tc * L_TC
  + λ_bce * L_presence
  + λ_proto * L_prototype
  + λ_kd * L_distill（后续实验再加入）
```

- `L_set`：七集合层次化负对数似然，是主损失，直接对齐 EMR。
- `L_TC`：数量交叉熵。
- `L_presence`：原有三个类别的多标签辅助损失，让所有数据继续学习类别存在性。
- `L_prototype`：共享类别原型/监督对比损失，重点拉开 Cargo 与 Tanker。
- `L_distill`：现有教师软目标蒸馏，必须在基础结构验证有效后再加入。

### 8.4 Cargo/Tanker 类别表征改进

只换集合头可能有帮助，但不足以保证达到 85%，因为给定真实数量时身份准确率仍只有约 80%。建议：

- 为 Tanker、Cargo、Tug 建立共享类别原型。
- 单目标类别 Query 向对应类别原型靠近、远离其他类别。
- 双目标中每个存在类别的 Query 继续对齐同一原型。
- 使用监督对比、余弦间隔或原型一致性损失，不使用简单类别权重替代。
- 如果 Train 中有 `s1/s2/s3`，可让混合信号中的类别 Query 对齐对应干净物理分量的编码特征。

训练期使用干净分量进行辅助监督在学术上允许，条件是：

- 只使用 Train 的干净分量；
- 推理仍然只输入 mix；
- 不使用 Val/Test 干净源帮助最终预测；
- 论文明确说明训练期辅助信息来源。

---

## 9. 数据侧最终方向

### 9.1 第一步不是扩数据，而是错误审计

下一步应将模型预测与 `all_info.txt` 联合，至少输出：

- 每条原始录音的样本数、准确率和错误方向；
- Cargo → Tanker 最严重的原始录音；
- Tanker → Cargo 最严重的原始录音；
- 各类别对准确率；
- 按 SIR 区间、SNR、距离、源深、重叠时长分组的错误率；
- 错误是否集中于少数船舶/录音，还是遍布整个类别。

最好先用 Train 的 OOF（out-of-fold）预测做主要审计；当前 Val 已被多次观察，只作为开发集辅助确认。

判断原则：

- 少数 Cargo 录音贡献大部分错误：优先解决录音覆盖、标签纯度或船舶身份偏差。
- 所有 Cargo 录音普遍混淆：优先改共享特征和类别判别损失。
- 错误集中在低 SIR、特定距离或重叠区间：定向补充这些物理条件。
- 错误与物理字段弱相关：更可能是 Cargo/Tanker 本身声学边界重叠或原始标签粒度问题。

### 9.2 核对类别映射与标签纯度

由于用户原先观察为 Cargo/Tug 混淆，而程序矩阵显示 Cargo/Tanker，V6 开发前必须：

1. 随机抽取 Cargo、Tanker、Tug 各若干样本；
2. 对照生成目录、`all_info.txt` 原始文件字段和模型标签；
3. 确认生成器 0/1/2 与模型 Tanker/Cargo/Tug 的交换映射无误；
4. 检查原始录音是否包含其他船舶、港口背景或标签不纯。

不能根据模型错误随意删除 Val/Test 样本。若发现客观标签污染，必须制定与模型预测无关的统一清洗规则，并对所有 split 一致执行，同时披露规则。

### 9.3 增加有效数据多样性

数据侧优先级：

1. **增加原始录音/船舶身份多样性**  
   不同 Cargo、Tanker 船舶、航速、工况和录音时段，比从同一录音反复生成更多窗口更重要。

2. **按原始录音均衡采样**  
   防止少数较长录音生成过多窗口并支配整个类别。类别计数平衡不等于录音身份平衡。

3. **只扩充 Train 的物理实现**  
   可增加新的 Bellhop 距离/源深、Wenz 噪声随机种子、包络起止、重叠时长及 SIR 候选，但必须保持既有物理约束。

4. **困难组定向采样**  
   只有在 OOF/Val 审计确认错误集中于某些物理区间后，才提高这些 Train 区间的采样概率。不能通过提高测试 SIR、去掉困难样本或修改目标幅度来获得更高分。

5. **真实 Sea State 3/多海况任务**  
   当前只有 SS2。若目标是海况 3 或跨海况，应基于明确的风速/噪声和传播环境分别生成 Train/Dev/Test，并冻结每个条件的评估分布；不能把 SS2 目录重命名为 SS3。

### 9.4 不再优先尝试的数据策略

- 高斯白噪声增强：已无收益或略差。
- 100% ±10% speed perturb：显著下降。
- 仅增加 Cargo 类别权重：类别数量本来平衡，可能把错误从一个方向推到另一个方向。
- 只复制现有 Cargo 样本：不会增加有效信息。
- 为提高 headline 修改 Val/Test SIR 或删除困难录音：不允许。

---

## 10. V6 推荐实验顺序

所有实验使用相同数据、相同训练预算和 seed 42/43/44，逐项单变量比较。

### D0：数据与标签审计（不训练）

- 核对 Cargo/Tanker/Tug 映射。
- 输出预测到 `all_info.txt` 的联合 CSV。
- 分析原始录音、SIR、SNR、距离、深度和重叠时长。

### V6-A：条件集合分类头

- 保留 E8 的共享主干配置。
- 移除旧残差专家。
- 加入 TC + 单目标三分类 + 双目标类别对三分类。
- 使用集合联合概率推理。
- 不加蒸馏，不加新数据，不加原型损失。

目的：单独验证训练目标与 EMR 对齐的收益。

### V6-B：共享类别原型/监督对比

- 在 V6-A 上增加类别原型约束。
- 重点观察 Cargo 单目标、Cargo+Tug 以及 Cargo/Tanker 互换错误。
- 不能只看总体 EMR。

### D1：录音均衡与困难物理 Train 扩充

- 根据 D0 结果扩充 Train。
- 保持 Val/Test 完全不变。
- 单独比较 V6-B 在旧 Train 与新 Train 上的差异。

### V6-C：训练期混合—干净源特征一致性

- 使用 Train 的 s1/s2/s3 辅助类别 Query 对齐。
- 推理输入保持不变。
- 先做轻量特征一致性，不立即构建庞大的完整源分离网络。

### V6-D：重新加入蒸馏

- 在 V6 结构自身被证明有效后，再训练结构兼容、具有多样性的教师。
- 蒸馏集合分布和类别存在概率。
- 先报告单学生，再报告可选的多种子推理集成。

### 后备方向

如果 V6-B/D1 后仍明显低于目标，再从以下方向选一个单独验证：

- 多尺度 Query：读取骨干中间层和末端层，保留细粒度低频线谱；
- 物理谐波/谱峰编码：显式建模线谱峰和谐波间距；
- 更完整的类别条件掩码或源分解辅助任务。

不要同时加入多尺度、源分解和新数据，否则无法判断收益来源，也容易被认为堆料。

---

## 11. 学术与工程边界

### 11.1 蒸馏不是作弊

以下条件下，集成蒸馏是正规方法：

- 教师只在 Train 上训练；
- Val 只用于模型选择；
- Test 不参与蒸馏、阈值或权重选择；
- 论文同时报告无蒸馏基线、教师集成、蒸馏学生和 TC 解码；
- 明确教师数量、学生结构、蒸馏损失和推理成本。

### 11.2 如何避免“堆料”嫌疑

论文核心不应罗列所有尝试过的模块。推荐主线叙述：

1. 双频带时频表征与共享类别 Query；
2. 数量条件化的结构化集合判决；
3. 共享类别原型促进单/双目标知识迁移；
4. 训练期集成蒸馏压缩到单模型。

Gate、双流融合、DEMON slice、白噪声和速度扰动作为消融/负结果简要报告，不进入最终模型贡献列表。EMA、Focal、归一化属于训练细节，不包装成独立创新。

### 11.3 评估纪律

- 当前 Val 已被多次查看，应视为 Development set。
- 最终结论需要一个未用于反复选择的新 Test 或新冻结数据集。
- 报告至少三个 seed 的均值和标准差，而不是只挑最好 seed。
- 报告总体 EMR、single/double EMR、七集合准确率和身份混淆矩阵。
- 如果报告推理集成，必须与单模型结果分开。
- 不在 Test 上搜索阈值、集合权重或 ensemble 权重。
- 不通过重新生成更容易的 Test、改变 SIR 分布或挑选随机种子跨过 85%。

---

## 12. 已修改的诊断工具

### 12.1 `compare_val_checkpoints.py`

位置：

```text
E:\Paper\Code\MUART V5.0\compare_val_checkpoints.py
```

已具备：

- Val-only；不构造、不读取 Test；
- 恢复 checkpoint 中的 resolved config；
- 将 JSON 中被字符串化的类别映射键恢复为整数，修复 `KeyError: 0`；
- 优先加载 `ema_state_dict`，保证 replay 与 saved Val 匹配；
- raw EMR、分类型 EMR；
- TC top-k、true-count oracle、base/residual 诊断；
- 多模型错误重合、概率平均集成、oracle；
- `--set-diagnostics`：七集合准确率、单目标身份混淆、双目标类别对混淆。

### 12.2 测试文件

```text
E:\Paper\Code\MUART V5.0\test_compare_val_checkpoints.py
```

包含 config 映射恢复、EMA 优先、TC constrained decode、单/双目标身份混淆测试。

当前 E:\Paper 默认 Python 没有安装 PyTorch，因此这里只完成了 `py_compile` 语法检查；训练电脑的 `xiuzhengyang` 环境可以运行模型诊断。此前出现的 `libiomp5md.dll already initialized` 不应通过永久设置 `KMP_DUPLICATE_LIB_OK=TRUE` 掩盖；它是环境中重复 OpenMP runtime 问题。正常使用比较脚本不需要运行测试文件。

### 12.3 当前训练电脑诊断命令

```powershell
python .\compare_val_checkpoints.py --data-dir "D:/LSJ/Code/UART/MUART V4.0/data_cargo_sl_sir15/SS2_bp12/dataset" --ckpt "E8_s42=D:/LSJ/Code/UART/MUART V5.0_runs/checkpoints_v5_distill_E8_white_off_s42/best_model.pt" --ckpt "E8_s43=D:/LSJ/Code/UART/MUART V5.0_runs/checkpoints_v5_distill_E8_white_off_s43/best_model.pt" --ckpt "E8_s44=D:/LSJ/Code/UART/MUART V5.0_runs/checkpoints_v5_distill_E8_white_off_s44/best_model.pt" --set-diagnostics
```

---

## 13. 新对话应从哪里继续

新对话不要重新做 E3–E8 的普通模块筛查，也不要立即增加更多同构教师或随机增强。

建议下一条任务是：

> 先阅读 `E:\Paper\记忆.md`，然后检查 `data_gen_v2` 的 `all_info.txt` 结构以及 `MUART V5.0` 的数据集样本索引，新增一个只读错误审计工具，将 checkpoint 的 Val/Train-OOF 预测与物理元数据关联，输出类别映射核对、每条原始录音准确率、Cargo/Tanker 错误方向、SIR/SNR/距离/深度/重叠时长分组统计。不要读取 Test，也不要修改训练代码。

完成 D0 后，再实施 V6-A 条件集合分类头。V6 的首要目标不是盲目扩大模型，而是在全部数据共享表征的前提下，直接优化七个合法集合并修复 Cargo/Tanker 类别边界。

---

## 14. 2026-09-06 权威续记：覆盖第 13 节的旧继续点

本节记录第 13 节以后已经完成的 D0、V6-A1 与 V6-A2 工作。新对话应以本节及其后各节为当前状态，不再从第 13 节建议的 D0 起点重新开始。

### 14.1 不得改变的实验边界

- 不重新进行 E3–E8；这些实验已经完成。
- 当前阶段禁止构造、读取或使用 Test；D0、V6-A1 和 V6-A2 的开发与核验均未读取 Test。
- Val 已被多次使用，属于开发集；最终论文结论仍须由以后一次性解封的新 Test 或新冻结数据支持。
- 用户已明确决定保留双源样本的 10–15 dB SIR，不再以删除该区间为研究方向。
- P6 人工成对听音不是当前必做步骤。它原本只用于对传播伪影做额外因果确认，不能再被描述成决定是否保留 10–15 dB SIR 的实验，也不应阻塞模型研究。

### 14.2 两台电脑的固定协作方式

项目文件在两台电脑上都有副本，但职责不同：

- 当前与助手对话、审阅报告、修改和生成代码的电脑使用 `E:\Paper`。
- 用户主要在另一台训练电脑的 VS Code 中运行程序，已经进入项目目录并选择好 Python 环境 `xiuzhengyang`。
- 训练电脑代码目录：`D:\LSJ\Code\UART\MUART V5.0`。
- 训练电脑数据项目目录：`D:\LSJ\Code\UART\MUART V4.0`。
- 训练电脑数据集目录：`D:\LSJ\Code\UART\MUART V4.0\data_cargo_sl_sir15\SS2_bp12\dataset`。
- 训练电脑运行结果根目录：`D:\LSJ\Code\UART\MUART V5.0_runs`。
- 原始 DeepShip 目录：`D:\LSJ\Data\DeepShip`。

以后只要助手生成或修改代码，就必须同时给出以下五项，不能只给方案：

1. 这次需要从 `E:\Paper` 电脑移植的**准确文件清单**；
2. 每个文件在训练电脑上的**准确目标目录**；
3. 明确说明是新增、替换还是保留，不允许笼统要求覆盖整个目录；
4. 用户在已选好环境的 VS Code 终端中应执行的**完整命令和严格运行顺序**；
5. 预期生成的结果目录，以及需要传回 `E:\Paper` 电脑的文件或整个文件夹。

默认采用逐文件复制，不搬移或覆盖整个项目文件夹；不得覆盖非空正式实验输出目录。若目标输出目录已存在且非空，应停止并报告，而不是自动删除。

---

## 15. D0 数据错误审计：已完成的探索与结论

### 15.1 D0 阶段状态

- P0–P5 为有效的主审计链，已经完成。
- P6 已取消为必做项；未完成人工听音不影响“保留 10–15 dB SIR”的决定。
- P7/P8 是辅助域与泄漏审计；P9 与已有类别暴露统计重复，不应继续扩展。
- 不能再次要求用户重复 P0、重新传 P4，或把 P6 误说成 SIR 决策的必要步骤。

### 15.2 已排除的问题

- 数据索引、`all_info`、类别映射、WAV 协议、PCM 内容和源映射未发现足以解释总体错误的系统性错误。
- P1 对 42 个 Val 样本进行了反事实重构，核查了源、噪声、目标数、SIR 和源顺序。
- P2 覆盖全部 Val：2463 个样本、3597 个源实例、1744 个模板、63 条原始录音。
- P3 将传播前模板与传播后保存源配对。在 130 个持续分歧模板中，78 个传播前已经错误，52 个传播前正确但传播后错误，72 个传播前后错误类别一致。
- P4 的录音级频谱签名证据有限，不支持“固定源位置泄漏”是主因。
- P8 使用训练电脑一致的映射哈希确认 Train/Val 在源文件、录音和模板层面无重叠。
- P9 显示类别正样本暴露总体平衡；单纯类别数量失衡不是主要解释。

### 15.3 已定位的主要问题

主问题是 Cargo/Tanker 的身份边界，且传播对 Cargo 的伤害远高于其他类别：

- P4 传播前源实例准确率：3005/3597 = 0.8354。
- P4 传播后源实例准确率：2891/3597 = 0.8037。
- Cargo 传播前准确率约 0.7798，传播后约 0.5696。
- Cargo 传播伤害：293/935 = 31.3%。
- Tanker 传播伤害：32/893 = 3.6%。
- Tug 传播伤害：37/1177 = 3.1%。
- P7 显示伤害最高的 5 条 Cargo 录音贡献约 70.99% 的伤害，但 17 条 Cargo 录音都存在伤害，因此既有录音域集中性，也有全类边界问题，不能简单删除少数录音解决。

### 15.4 为什么保留 10–15 dB SIR

P5 的分层结果不支持 10–15 dB 是独立坏区间：

- Cargo 在 0–5、5–10、10–15 dB 的传播伤害率约为 31.5%、34.6%、34.7%。
- 10–15 dB 与其他双源 SIR 区间的总体风险差约 0.001329。
- Cargo 内部风险差约 0.01714，仍低于预设 0.05 判断阈值。
- 高 SNR 也没有出现稳定更差、足以要求修改生成范围的错误模式。

结论：保留 10–15 dB SIR；后续从共享类别证据、单目标识别和过拟合控制着手，而不是通过删除较难数据来制造性能提升。

---

## 16. 当前基线口径与 V6-A1 结果

### 16.1 必须区分的三个指标

- E6 s42（无蒸馏单模型）+ TC：Val EMR = 0.817296。
- E8 s42（蒸馏学生，推理时仍为单模型）+ TC：Val EMR = 0.820544。
- E8 s42/s43/s44 三个学生做概率平均并使用 TC：Val EMR = 0.827040。这才是多模型推理集成结果。

因此，0.827040 不是单模型性能；E8 的“多模块蒸馏”是训练阶段知识来源，单个学生在推理时仍是一个模型。

### 16.2 V6-A1 的真实结果

报告目录：

```text
E:\Paper\Code\V6A_reports\checkpoints_v6a_condset_s42
```

V6-A1 使用原 Train/Val、s42、从头训练、无蒸馏、无推理集成、未读取 Test。最佳 epoch 30：

- Val EMR = 0.8185140073。
- count accuracy = 0.99918798。
- noise = 1.000000。
- Tanker = 0.806167。
- Cargo = 0.682819。
- Tug = 0.960352。
- Tanker+Cargo = 0.823045。
- Tanker+Tug = 0.812757。
- Cargo+Tug = 0.701646。

相对 E6 s42+TC 仅增加约 0.001218，即约 3 个 Val 样本；比 E8 s42+TC 少约 0.00203，即约 5 个样本；比 E8 三种子推理集成+TC 少约 0.008526，即约 21 个样本。V6-A1 没有带来实质提升。

### 16.3 V6-A1 的过拟合证据

此前没有及时向用户明确报告过拟合，这是必须纠正的研究记录：

- Val NLL 在 epoch 16 左右最低，约 0.476757。
- 最高 Val EMR 出现在 epoch 30，为 0.818514，但此时 Val NLL 已升至约 0.55388。
- epoch 50 时 Train EMR 约 0.96484，Val EMR 约 0.80715，Val NLL 约 0.82007。

这说明模型在训练后期越来越自信但泛化变差。以后不能只汇报 EMR 和 patience，还必须同步监测 Val NLL、错误置信度、Brier、固定 Train 子集与 Val 的差距，以及单目标三类指标。

### 16.4 V6-A1 遗漏的两处原始设计

V6-A1 没有完整实现原计划：

1. 缺少“全部样本上的原始三类 presence BCE 辅助损失”；
2. 双目标集合分数由较独立的 pair MLP 产生，没有严格由共享的两个单类证据加小交互项组成。

这削弱了单目标知识向双目标判决的传递，也使“先改善单目标，再改善多目标”的主线无法被真正检验。

---

## 17. 数据生成侧的计划与泄漏边界

D1 已实现并正式运行：先选择原始录音，再从该录音内部选择模板，用更均衡的录音曝光替代模板数量对抽样概率的隐式支配。V6-A2 仍使用原 Train，V6-D1 只改变 Train 的源采样方式，结构、seed、冻结 Val 和物理协议不变。正式结果见第 21 节。

“先选录音、再选模板”本身不会必然造成数据泄漏，但必须同时满足：

- 先冻结录音级 Train/Val/Test 划分，再在各自池内抽样；
- 同一原始录音的所有切片、模板、传播版本和混合版本只能属于一个 split；
- 双源混合的两个源都必须来自 Train 录音池，不能跨 split 取源；
- 录音 ID 不能作为模型输入；归一化统计只能由 Train 估计；Val 必须保持冻结；
- 若原数据存在真实 vessel ID，同一艘船跨不同录音仍可能造成身份泄漏，只有录音互斥并不能证明 vessel 身份互斥；应在有 vessel ID 时做额外分组审计；
- SIR/可用时长等拒绝采样可能让最终被接受样本重新偏向少数录音，因此必须审计最终成品数据，而不只审计采样器的理论概率。

研究顺序已经按计划完成：先完成原 Train 的 V6-A2，再单独完成 D1 录音均衡消融；没有把结构与新数据同时改掉。

---

## 18. V6-A2：实现与验证记录（正式训练已经完成）

### 18.1 目标与结构

用户已批准：在原 Train 上，以 s42 修补 V6-A1 的两处设计，同时显式监控过拟合和单目标性能。

V6-A2 的关键设计：

- 单类/presence logits 被单目标判决、全样本 BCE 和双目标组合共同使用。
- 双目标分数为 `e_i + e_j + 0.5 * tanh(r_ij)`。
- `r_ij` 来自共享、对称的小型交互 MLP，hidden size = 32，最后一层零初始化，使初始双目标证据严格等于两个单类证据之和。
- 总损失：`set NLL + 0.5 * all-sample presence BCE + 0.1 * query orthogonality`。
- 使用原 Train/Val，不做蒸馏，不加新数据，不读 Test，从头训练。
- 训练预算与 A1 对齐：60 epochs、batch 32、patience 20、lr 3e-4、EMA。

这是“两处相互关联的设计纠正包”，可以与 A1 做整体对照，但不能把结果伪装成对两个组件各自独立贡献的严格消融。若 A2 有效，后续才拆分组件；若无效，再依据诊断决定是单目标课程训练、正则化，还是进入 D1。

### 18.2 过拟合与单目标监测

- 每个 epoch 使用同一个 EMA 模型评估 Val 和固定、无增强的 Train 子集。
- 固定 Train 子集对七个合法集合每类最多取 32 个，共最多 224 个；监测会保存并恢复随机数状态，不改变训练轨迹。
- 每轮记录 Tanker、Cargo、Tug 三个单类集合，single 总体和 double 总体。
- 每轮记录 Val NLL、all-sample BCE、错误样本平均置信度、Brier 和 true-count 条件身份准确率。true-count 只用于诊断，不参与模型选择或推理报告。
- 若连续 5 轮 Val NLL 高于历史最低值 0.05 以上、同时 TrainEval NLL 继续改善，则发出 overfit alert。
- 主 checkpoint 仍严格按最高 Val EMR 选择并使用 patience 20；另存 `best_nll_model.pt` 只作诊断，不允许训练后在多个 checkpoint 中按想要的结论挑选。
- `history.csv` 和逐轮指标每轮落盘，避免只剩最佳点而无法判断过拟合。

### 18.3 已完成的代码与验证

当前电脑文件位于：

```text
E:\Paper\Code\MUART V5.0\config_v6a2.py
E:\Paper\Code\MUART V5.0\model_v6a2.py
E:\Paper\Code\MUART V5.0\train_v6a2.py
E:\Paper\Code\MUART V5.0\test_v6a2.py
E:\Paper\Code\MUART V5.0\run_v6a2.ps1
E:\Paper\Code\MUART V5.0\V6A2_SHARED_EVIDENCE.md
```

验证结果：

- V6-A1 原有 6 项测试与 V6-A2 新增 9 项测试合计 15 项通过。
- 新测试覆盖：pair 初始加性、交互边界、双目标 NLL 对单类 scorer/骨干的梯度、presence BCE 对全部行的梯度、保留 encoder 初始化与减少 pair 容量、监测禁用增强并恢复 RNG、拒绝 Test、过拟合告警、一轮完整训练/保存/重载且不读取 Test。
- 本机真实 Train/Val smoke 通过：Train 11490、Val 2463、Test 未加载，A2 参数量 4,746,885。
- 本机 smoke 目录 `E:\Paper\Code\V6A_reports\V6A2_smoke_local_20260906` 只属于工程验证，不是研究结果。
- 与 A1 的协议检查通过：数据路径/样本数清单、归一化和关键比较配置一致；A1 参考 EMR 为 0.818514。

---

## 19. V6-A2 在训练电脑上的历史移植与运行顺序

### 19.1 本次准确移植清单

从当前电脑 `E:\Paper\Code\MUART V5.0` 逐文件复制以下 6 个文件：

```text
config_v6a2.py
model_v6a2.py
train_v6a2.py
test_v6a2.py
run_v6a2.ps1
V6A2_SHARED_EVIDENCE.md
```

目标目录统一为：

```text
D:\LSJ\Code\UART\MUART V5.0
```

只复制这 6 个文件，不覆盖整个项目目录。V6-A2 还会调用训练电脑已经存在的 V6-A1 文件 `config_v6a.py`、`model_v6a.py`、`train_v6a.py`、`test_v6a_conditional_set.py`，以及 V5 文件 `config.py`、`model.py`、`dataset.py`、`experiment_snapshot.py`；这些不是本轮新增文件，不需要再次移植，除非检查阶段明确报告缺失。

### 19.2 VS Code 终端严格运行顺序

用户已经选择好环境，可直接在 VS Code PowerShell 终端逐条运行。前一步没有 PASS 时不要继续下一步。

```powershell
cd "D:/LSJ/Code/UART/MUART V5.0"
```

第一步，完整协议与单元检查：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "D:/LSJ/Code/UART/MUART V5.0/run_v6a2.ps1" -Mode Check
```

第二步，真实数据短 smoke：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "D:/LSJ/Code/UART/MUART V5.0/run_v6a2.ps1" -Mode Smoke
```

第三步，只有 Check 与 Smoke 都通过后才正式训练：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "D:/LSJ/Code/UART/MUART V5.0/run_v6a2.ps1" -Mode Train
```

正式输出目录：

```text
D:\LSJ\Code\UART\MUART V5.0_runs\checkpoints_v6a2_shared_evidence_s42
```

A1 对照目录应为：

```text
D:\LSJ\Code\UART\MUART V5.0_runs\checkpoints_v6a_condset_s42
```

若正式输出目录已经非空，脚本应拒绝覆盖；不要删除旧结果，应把实际提示发回后再决定新目录名。

### 19.3 运行后传回

正式训练完成后，将整个目录：

```text
D:\LSJ\Code\UART\MUART V5.0_runs\checkpoints_v6a2_shared_evidence_s42
```

传回当前电脑：

```text
E:\Paper\Code\V6A_reports\checkpoints_v6a2_shared_evidence_s42
```

下一轮应首先审阅 `comparison.md`、`history.csv`、单/双目标分层指标、Cargo/Tanker 混淆、过拟合告警和 best-EMR 与 best-NLL 的差异，再决定下一步。不要在未审阅 A2 正式结果前直接实施 D1 或添加蒸馏。

---

## 20. 历史继续点（已经完成）

第 18–19 节所述 V6-A2 正式训练、结果传回和审阅均已完成；随后 V6-D1 也已经完成。后续对话不得再把 A2 或 D1 当成待运行实验，也不得要求用户重复迁移或训练。

---

## 21. V6-D1 正式结果：录音均匀抽样未突破 Cargo

### 21.1 实验位置与协议

正式结果目录：

```text
E:\Paper\Code\V6A_reports\checkpoints_v6d1_recording_uniform_s42_20260906_150607_576
```

D1 使用与 V6-A2 相同的模型、seed=42、物理生成协议和冻结 Val；唯一实验变量是 Train 从模板均匀抽样改为“先均匀选原始录音，再在录音内选模板”。Test 未加载、未枚举、未评估。

数据审计为 PASS：

- Cargo 录音曝光 CV：0.4725 → 0.1178。
- Tanker 录音曝光 CV：0.8599 → 0.1656。
- Tug 录音曝光 CV：0.4821 → 0.0999。
- 三类 D1 accepted coverage 均为 1.0。
- Train/Val 的模板、录音和 `Parent_Folder` 代理均无重叠。
- 冻结 Val 的参考与 D1 SHA-256 都是 `aacc22b90dfbad1b202874746a8ab081bc5960f9c5f6c1ce0e83de86064f2f66`。
- 当前元数据没有真实 vessel ID，因此只能证明录音和 `Parent_Folder` 代理隔离，不能证明真实船舶身份隔离。

这说明 D1 的干预确实生效；其阴性结果不能归因于“采样器没有改成功”或 Val 改变。

### 21.2 预注册主 checkpoint 对照

D1 主 checkpoint 严格按最高 Val EMR 选择，为 epoch 46：

| 指标 | V6-A2 原 Train | V6-D1 recording-uniform | 变化 |
|---|---:|---:|---:|
| Overall EMR | 0.809582 | 0.820138 | +0.010556 |
| noise | 1.000000 | 1.000000 | 0 |
| Tanker 单目标 | 0.823789 | 0.814978 | -0.008811 |
| Cargo 单目标 | 0.647577 | 0.634361 | -0.013216 |
| Tug 单目标 | 0.969163 | 0.973568 | +0.004405 |
| Tanker+Cargo | 0.825103 | 0.843621 | +0.018519 |
| Tanker+Tug | 0.788066 | 0.812757 | +0.024691 |
| Cargo+Tug | 0.683128 | 0.701646 | +0.018519 |
| single 总体 | 0.813510 | 0.807636 | -0.005874 |
| double 总体 | 0.765432 | 0.786008 | +0.020576 |

D1 的收益集中在双目标，单目标总体和 Cargo/Tanker 都没有改善。因此总体 EMR 上升不能被解释为 Cargo 单目标突破。

### 21.3 预注册 D1 判定

| 诊断计数 | V6-A2 | D1 | D1 变化 | 强恢复阈值 |
|---|---:|---:|---:|---:|
| Cargo correct / 227 | 147 | 144 | -3 | >=155 |
| 单目标 Cargo/Tanker 互换 | 87 | 90 | +3，恶化 | <=76 |
| 含 Tug 双目标的 Cargo/Tanker 互换 | 222 | 214 | -8，部分改善 | <=205 |
| Overall correct / 2463 | 1994 | 2020 | +26 | 不退化 |

正式标签是 `recording_bias_not_supported`；`diagnostic_success=false`，`strong_breakthrough=false`。D1 作为“解决 Cargo 单目标与 Cargo/Tanker 身份边界”的干预失败，但不是无价值实验。

在同一批 2463 个冻结 Val 样本上做配对核算：D1 修复 A2 错误 171 个，同时破坏 A2 正确样本 145 个，净增 26；双侧精确 McNemar/binomial p 约 0.16。各合法集合的配对变化也均未形成强证据。因此不能把约 1.06 个百分点总体提升当成已经确认的稳定收益。

### 21.4 全训练轨迹与过拟合

- 最佳 Val NLL 在 epoch 12：EMR=0.814860，NLL=0.432833，Cargo=0.590308。
- Cargo 的全程最高点在 epoch 25：151/227=0.665198，仍未达到预注册 155/227=0.682819；当时总体 EMR=0.816484。
- 主 checkpoint epoch 46：EMR=0.820138，NLL=0.839975，Cargo=0.634361，TrainEval EMR=1.0。
- epoch 60：EMR=0.817296，NLL=1.018794，Cargo=0.603524，TrainEval EMR=1.0。
- V6-A2 主 checkpoint NLL=0.639756，D1 主 checkpoint NLL=0.839975。
- 错误样本平均置信度从 A2 的 0.816295 上升到 D1 的 0.891796；set Brier 从 0.303827 恶化到 0.311014。
- 没有任何一个 epoch 同时满足 D1 的全部强恢复条件。

结论：D1 后期严重记忆 Train，错误更自信；Cargo 偶尔在中期短暂高于 A2，但不稳定、未达到阈值，也不能事后按 Cargo 指标另选 checkpoint。

### 21.5 科学解释与决策

D1 否定的是“模板数量导致少数录音过度曝光，是 Cargo 单目标差的主要原因”这一假设。逐录音均匀化不是 Cargo/Tanker 核心问题的充分解法。当前更可能的瓶颈是：

- Cargo 与 Tanker 原始声学边界本身重叠或标签粒度不足；
- 传播、混合或特征生成对 Cargo 的判别线索破坏更强；
- 模型能检测 Cargo presence，但难以稳定确定 Cargo/Tanker 身份；
- 有效 vessel 身份多样性不足，而不是模板/录音计数不均衡；
- 模型容量足以记忆 Train，但正则化和泛化仍不足。

D1 不进入 Test；保留为可信的阴性数据消融和后续 D2 的对照。不得继续通过重复录音均匀化、简单复制 Cargo 或只调类别权重来解决问题。

### 21.6 当前代码包状态

`E:\Paper\Code\MUART V5.0` 已整理为 D1 最小可运行包，共 129 个文件、约 41.39 MB：

- `data_gen_v2` 已内置，包含必需 `.arr_gen`；
- D0/P0-P9、旧评估、诊断、旧 checkpoint、缓存和重复文档已移出运行包；
- 保留代码扫描无 `D:\...`、`E:\...`、`MUART V4.0` 等机器写死路径；
- `run_v6d1.ps1` 默认从脚本位置解析 ModelDir 与 `data_gen_v2`；
- 外部路径通过 `-DataRoot`、`-OutputRoot`、`-ReferenceRun` 显式传入，显式参数优先于环境变量；
- 历史 Git 与旧材料保存在 `E:\Paper\Code\MUART_V5_cleanup_archive_20260906`，不属于迁移运行包；D0 独有证据已移至 `E:\Paper\Code\D0_reports`。

---

## 22. 当前唯一继续点：DeepShip 组合与生成链可识别性

下一步不要直接训练另一个完整模型，也不要加载 Test。先设计一个冻结、只读、低成本的可识别性审计，对 DeepShip 四种三类别组合使用相同 split、样本预算、特征和随机种子：

1. Cargo + Tanker + Tug（当前组合）；
2. Cargo + Tanker + Passenger；
3. Cargo + Tug + Passenger；
4. Tanker + Tug + Passenger。

审计必须分成两层：

- 原始源录音/模板层：判断不经过传播混合时各类别是否可分；
- 当前生成链输出层：判断 Bellhop、包络、噪声、SIR 和特征提取后可分性下降多少。

重点比较单目标混淆矩阵、Cargo/Tanker 双向替换、每录音/`Parent_Folder` 表现、置信度和生成前后性能差。若 Cargo/Tanker 在源层已经差，优先处理标签、类别定义和真实 vessel 多样性；若源层可分而生成后明显变差，优先修改传播/混合/特征生成；若换入 Passenger 后显著改善，则可确认当前 Cargo/Tanker 类别对是主瓶颈。

在该审计完成前，不应启动 D2 正式训练。任何新代码仍必须附带移植清单、目标目录、严格运行顺序、预期输出和传回目录。
