# 新窗口继续提示词（V6-A2）

请完整阅读 `E:\Paper\记忆.md`，以第 14–20 节作为当前权威项目状态并继承全部实验结论。第 13 节“下一步从 D0 开始”已经过时，不要照它重复实验。

必须遵守以下边界：

1. 不重新进行 E3–E8，不重复 D0/P0–P5，不要求补做 P6；P6 人工听音已经取消为必做项。
2. 不构造、不读取、不使用 Test。Val 已是开发集。
3. 已决定保留 10–15 dB SIR，不再讨论通过删除该区间提高性能。
4. 当前问题是 Cargo/Tanker 身份边界、传播对 Cargo 的显著伤害、单目标性能不足和 V6-A1 的明显过拟合。
5. 严格区分：E8 s42+TC 是单个蒸馏学生推理，Val EMR 0.820544；E8 s42/s43/s44+TC 才是三模型推理集成，Val EMR 0.827040；V6-A1 为无蒸馏单模型，Val EMR 0.818514。
6. 数据生成侧“先选原始录音、再选模板”的 D1 尚未实现。当前先在原 Train 上完成 V6-A2，避免同时改结构和数据。若以后做 D1，必须先冻结录音级 split、仅在 split 内抽样、审计 vessel/录音/模板重叠和最终接受样本分布。

两台电脑的工作方式必须始终记住：所有项目文件在两台电脑上都有副本；我在当前电脑与助手对话和接收修改，路径以 `E:\Paper` 开头；我主要在另一台电脑的 VS Code 中运行程序，已经进入项目并选择好 `xiuzhengyang` 环境，训练电脑代码目录为 `D:\LSJ\Code\UART\MUART V5.0`，数据项目为 `D:\LSJ\Code\UART\MUART V4.0`，运行结果根目录为 `D:\LSJ\Code\UART\MUART V5.0_runs`，原始 DeepShip 为 `D:\LSJ\Data\DeepShip`。

因此，每次你生成或修改任何代码时，必须直接给出：

- 需要从当前电脑移植到训练电脑的准确文件清单；
- 每个文件的准确目标路径，以及新增/替换/保留说明；
- 我在已选好环境的 VS Code PowerShell 终端中要执行的完整命令；
- 严格运行顺序、每一步成功标准和失败时应停止的位置；
- 训练电脑上的输出目录；
- 需要传回当前电脑 `E:\Paper` 下的准确文件或整个目录。

不要只给设计方案，也不要让我复制覆盖整个项目目录。默认逐文件移植；正式输出目录若已非空，不得擅自删除或覆盖。

当前已生成并验证 V6-A2 六个文件：`config_v6a2.py`、`model_v6a2.py`、`train_v6a2.py`、`test_v6a2.py`、`run_v6a2.ps1`、`V6A2_SHARED_EVIDENCE.md`。它修复 V6-A1 的两处缺失：全部样本 presence BCE，以及由共享单类证据加小交互项构造双目标分数；还加入固定 Train 子集/Val 的过拟合和单目标分层监测。本机已有 15 项测试通过、真实 Train/Val smoke 通过，未读取 Test。

请先检查正式结果是否已经传回：

`E:\Paper\Code\V6A_reports\checkpoints_v6a2_shared_evidence_s42`

如果已经存在并包含正式结果，请完整审阅 `comparison.md`、`history.csv`、预测与分层指标，回答：

- V6-A2 相对 V6-A1 是否有超过随机波动的改进；
- Tanker/Cargo/Tug 三个单目标集合分别如何变化，Cargo/Tanker 混淆是否改善；
- 双目标提升是否与共享单类证据改善一致；
- best-EMR epoch 与 best-NLL epoch 差异、TrainEval/Val gap、错误置信度和 Brier 是否仍显示过拟合；
- 下一步只能选一个：继续做专门的单目标/正则化模型消融，或进入 D1 录音均衡数据生成。请基于证据选择，不能同时修改多项。

如果正式结果目录尚不存在，请不要做新设计，而是指导我在训练电脑依次运行：

```powershell
cd "D:/LSJ/Code/UART/MUART V5.0"
powershell -NoProfile -ExecutionPolicy Bypass -File "D:/LSJ/Code/UART/MUART V5.0/run_v6a2.ps1" -Mode Check
powershell -NoProfile -ExecutionPolicy Bypass -File "D:/LSJ/Code/UART/MUART V5.0/run_v6a2.ps1" -Mode Smoke
powershell -NoProfile -ExecutionPolicy Bypass -File "D:/LSJ/Code/UART/MUART V5.0/run_v6a2.ps1" -Mode Train
```

三步必须顺序执行，前一步未通过就停止。正式输出 `D:\LSJ\Code\UART\MUART V5.0_runs\checkpoints_v6a2_shared_evidence_s42`，完成后整个目录传回 `E:\Paper\Code\V6A_reports\checkpoints_v6a2_shared_evidence_s42`。
